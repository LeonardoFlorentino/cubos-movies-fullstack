{
  "name": "cubos-movies-fullstack",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "dev:backend": "npm run start:dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "lint": "npm run lint:check --workspace backend && npm run lint --workspace frontend",
    "test": "npm run test:ci --workspace backend && npm run test --workspace frontend"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.4",
    "resend": "^6.12.3",
    "tailwindcss": "^4.2.4"
  },
  "devDependencies": {
    "prettier-plugin-tailwindcss": "^0.8.0",
    "typescript": "^6.0.3"
  }
}
